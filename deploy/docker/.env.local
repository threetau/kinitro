# Shared environment for local Docker stack
#
# These defaults are non-sensitive and intended for local development only.

# Backend API key used by the validator service during authentication.
KINITRO_API_KEY=local-dev-key

# MinIO (S3-compatible) configuration shared by backend/evaluator.
S3_ENDPOINT_URL=http://minio:9000
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=kinitro-submissions
S3_REGION=us-east-1
S3_PUBLIC_URL_BASE=http://localhost:9000/kinitro-submissions
