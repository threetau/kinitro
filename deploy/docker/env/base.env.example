# Environment overrides for base validator deployments
# Copy to deploy/docker/env/base.env and adjust before running the stack.

KINITRO_API_KEY=change-me
# Additional settings may be added here (e.g., S3 credentials for evaluator)
