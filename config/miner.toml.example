version = "0.1.0"
log_level = "INFO" # Must be one of TRACE, DEBUG, INFO, WARN, ERROR

wallet_path = "~/.bittensor/wallets"
wallet_name = "default"
hotkey_name = "default"

# Backend API used for requesting upload slots and reporting results
backend_url = "https://api.kinitro.ai"

# Submission configuration
submission_dir = "./evaluator/submissions/default_submission"
# Optional: uncomment to override auto-generated version or hold-out window
# submission_version = "v1"

# Chosen competition ID
competition_id = "your-chosen-competition-id"

[subtensor]
network = "finney"
address = "wss://entrypoint-finney.opentensor.ai:443"
netuid = 26

[local_eval]
# Local sandbox run defaults (optional)
agent_host = "127.0.0.1"
agent_port = 8000
# agent_start_cmd = "uv run python submission_template/main.py --host 127.0.0.1 --port 8000"
ray_num_cpus = 2
ray_num_gpus = 0
local_results_dir = ".kinitro/miner_runs"
# Required: point to a JSON/TOML file with detailed specs (see config/benchmarks/local_mt10.json)
benchmark_spec_file = "config/benchmarks/local_mt10.json"
