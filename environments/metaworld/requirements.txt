# Requirements for MetaWorld evaluation environment
# These are installed in the Docker container

# HTTP client for calling miner endpoints
httpx>=0.25.0

# MuJoCo simulation
mujoco>=3.0.0

# MetaWorld environments
# Pinned to specific commit for reproducibility (includes camera width/height support)
metaworld @ git+https://github.com/Farama-Foundation/Metaworld.git@918cd0d3abb6c45f213d72730a06ef119d84218e

# Image processing for camera rendering
Pillow>=10.0.0
opencv-python>=4.8.0

# Numerics
numpy>=1.24.0

# Logging
structlog>=23.0.0

# Data validation (for rl_interface)
pydantic>=2.0.0

# Required by gymnasium (metaworld dependency)
packaging>=21.0
